<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zipzop">
        <activity
            android:name=".AbrirCaixaActivity"
            android:exported="false"
            android:label="Novo Caixa">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".CaixaActivity" />
        </activity>
        <activity
            android:name=".CaixaActivity"
            android:exported="false"
            android:label="Caixa">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".activity.SalvarProdutoActivity"
            android:exported="false"
            android:label="Novo Produto">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activity.ProdutoActivity" />
        </activity>
        <activity
            android:name=".activity.ProdutoActivity"
            android:exported="false"
            android:label="Produto">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".MainActivity" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>